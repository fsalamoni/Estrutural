{
  "name": "estrutural",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:next": "next build",
    "preview": "firebase emulators:start --only hosting",
    "deploy": "npm run build && npx firebase-tools deploy --project hocapp-44760 --only hosting,firestore:rules,storage",
    "start": "firebase emulators:start --only hosting",
    "lint": "eslint .",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@fontsource/material-symbols-outlined": "^5.2.43",
    "autoprefixer": "^10.5.0",
    "firebase": "^11.6.0",
    "next": "^16.2.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "^16.2.4",
    "firebase-tools": "^14.22.0",
    "postcss": "^8.5.10",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
